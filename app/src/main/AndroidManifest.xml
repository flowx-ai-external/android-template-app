<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="ai.flowx.external.android.template.app.TemplateApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTemplateApp"
        tools:replace="android:theme"
        tools:targetApi="31">
        <activity
            android:name="ai.flowx.external.android.template.app.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidTemplateApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ai.flowx.external.android.template.app.ProcessActivity" android:exported="false" />
    </application>

</manifest>